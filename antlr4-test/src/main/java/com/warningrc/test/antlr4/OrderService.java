package com.warningrc.test.antlr4;

public interface OrderService {

    /**
     * Checks if is matching.
     *
     * @param order the order
     * @return true, if is matching
     */
    boolean isMatching(Order order);
}
